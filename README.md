# Markdown DSL for python
Generates Gitlab / Github flavoured markdown
```
print ("\n".join([h1("Markdown DSL for python"),
				  "Generates Gitlab / Github flavoured markdown",
				  code(readme_example)]))
```
